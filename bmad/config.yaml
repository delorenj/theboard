# BMAD Method v6 - Project Configuration
# Project: TheBoard - Multi-Agent Brainstorming Simulation
# Migrated from .bmad/ structure: 2026-01-07

project_name: "TheBoard"
project_type: "api"  # CLI + API for multi-agent meetings
project_level: 2  # 17 stories across 6 sprints = Level 2 (5-15 stories per phase)

# Output folder (relative to project root)
output_folder: "docs"

# Module-specific settings
bmm:
  workflow_status_file: "docs/bmm-workflow-status.yaml"
  sprint_status_file: "docs/sprint-status.yaml"

# Path configuration
paths:
  docs: "docs"
  stories: "docs/stories"
  tests: "tests"
